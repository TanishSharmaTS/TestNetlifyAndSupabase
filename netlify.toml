[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# This is the correct way to disable secrets scanning
[build.processing]
  skip_secrets_scanning = true